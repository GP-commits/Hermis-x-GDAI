// Firebase's web app configuration is public; never put a service-account key here.
// The real project config is filled in when Firebase is connected.
export const firebaseConfig = null;

// Local integration tests replace this module in the browser. Production never
// connects to emulators, even if a URL query parameter requests it.
export const emulatorConfig = null;
